Mapping of Claude technique grouping to repository files (search results)

- Probabilistic / Statistical Reasoning
  - Bayesian inference: no direct match found in `src/`.
  - Markov models: no direct match found in `src/`.
  - Monte Carlo methods: no direct match found in `src/`.
  - Gaussian processes: no direct match found in `src/`.

- Search & Optimization
  - Genetic algorithms / evolutionary computation: no direct match found in `src/`.
  - Simulated annealing: no direct match found in `src/`.
  - Gradient descent: no direct match found in `src/`.
  - Heuristic search (A*, hill climbing): `src/heuristic.php`, `src/search.php` mention heuristics.

- Supervised Learning
  - Neural networks / deep learning: `src/neural-network.php`, `src/explainability.php`.
  - Decision trees & ensembles: `src/decision-tree-learning.php`.
  - Support vector machines: no direct match found in `src/`.
  - Regression analysis: no direct match found in `src/`.

- Unsupervised Learning
  - Clustering: no direct match found in `src/`.
  - Dimensionality reduction (PCA, autoencoders): no direct match found in `src/`.
  - Association rule learning: no direct match found in `src/`.

- Reinforcement Learning
  - Value-based / policy-based RL: no direct match found in `src/`.
  - Multi-armed bandits: no direct match found in `src/`.

- Symbolic / Logic-Based Reasoning
  - Expert / rule-based systems: `src/inference-engine.php`, `src/interpreter.php`, `src/blackboard.php`.
  - Constraint satisfaction: no direct match found in `src/`.
  - Fuzzy logic: no direct match found in `src/`.

- Graphical / Structured Models
  - Bayesian networks: no direct match found in `src/`.
  - Markov random fields / CRFs: no direct match found in `src/`.
  - Graph neural networks: no direct match found in `src/`.

Notes:
- The search looked for mentions and filenames related to the listed techniques under `src/`. Several techniques (Bayesian, Markov, Monte Carlo, etc.) do not have explicit pages; they may be discussed in broader articles or absent.
- Next step: I can (a) map techniques to existing pages more thoroughly (scan content and index pages), (b) add new pages for missing techniques under `src/`, or (c) generate a concise summary to insert into `todo.md`.
