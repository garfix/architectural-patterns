# Claude technique grouping integration

Date: 2026-08-16

Overview:
- Imported a comprehensive technique taxonomy from `claude-input.md` and integrated it into `src/template/data.php`.
- Added new category constants: `CAT_PROBABILISTIC`, `CAT_SEARCH_OPTIMIZATION`, `CAT_EVOLUTIONARY`, `CAT_SUPERVISED_LEARNING`, `CAT_DEEP_LEARNING`, `CAT_UNSUPERVISED_LEARNING`, `CAT_REINFORCEMENT_LEARNING`, `CAT_SYMBOLIC_REASONING`, `CAT_GRAPHICAL_MODELS`.
- Linked those as `children` under `CAT_PROBLEM_SOLVING`.

Data model notes:
- `categories[].patterns` must contain human-readable pattern `name` values (not slugs). This is required because the site looks up patterns by name.
- `patterns[]` entries include `name`, `image`, and `link` (slug). Keep `name` exact to avoid missing lookups.

Images:
- Created `src/images/bayesian-inference.svg` (282×282) and updated `src/template/data.php` to reference the SVG.
- Visual style: minimal boxes/arrows/icons; keep essence, not exhaustive info.

Outstanding validation / next steps:
- Run validation script to ensure every `categories[].patterns` name exists in `patterns[]`.
- Decide whether `src/template/functions.php` should accept slug lookups; currently site expects name-only.
- Optionally generate SVG placeholders for other patterns; follow the `images-inspection.md` style.

Who/When:
- Assistant will continue to add short notes here after meaningful changes.

--
Generated by the assistant.